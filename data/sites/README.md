# Site Data

This folder is the local place for site-based validation data.

Simple meaning: keep everything for one river/camera site together in one folder instead of spreading inputs, outputs, and human notes across many places.

## Folder Layout

Use one folder per site:

```text
data/sites/
  example-site/
    manifest.jsonl
    configs/
    inputs/
      videos/
      other/
    labels/
    human-evidence/
      flood-images/
      notes/
    outputs/
      validation-report.md
      records.jsonl
      summary.md
      operator-notes.txt
      label-comparison.md
      threshold-tuning.md
      review-images/
```

For real validation work, create a site folder with a safe short name:

```text
data/sites/colorado-river-windy-gap/
data/sites/bagmati-river-demo/
data/sites/koshi-river-demo/
```

## What Goes Where

`configs/` keeps site and camera config files.

Simple example: a config can say which camera ID was used and which `reference_region` should be watched.

`manifest.jsonl` lists the videos for that site.

Simple example: a manifest row can say `demo-river-001.mp4` is a practice video, has a human label, and is not approved for GitHub.

Use `practice` for videos we can learn from while changing code.

Use `locked_validation` for videos saved for fair checking after tuning.

`inputs/videos/` keeps local validation videos.

Simple example: one short clip from a river camera can live here while a developer tests the POC locally.

`inputs/other/` keeps other local inputs for that site.

Simple example: a calibration image, a reference frame, or a note about which frame was used to choose the reference region.

`labels/` keeps human labels.

Simple example: a reviewer may label a time window as `normal water`, `rising water`, `high water`, `unclear view`, or `camera moved`.

Use JSON Lines for labels, with one label record per line.

Safe example:

```json
{"video_id":"demo-river-001","time_window_seconds":[0,30],"human_label":"water_rising","confidence":"medium","note":"Water appears to move higher against the bridge pillar."}
```

Allowed `human_label` values are:

- `water_rising`
- `water_falling`
- `no_clear_change`
- `camera_video_problem`
- `cannot_judge`

See the Human Label Format documentation for the full field list and validation command.

After a local POC run, compare labels with system output using the Human Label Comparison guide.

Simple example: compare `labels/example-labels.jsonl` with `outputs/records.jsonl` to see whether the human label and system visual-change result agree.

`human-evidence/flood-images/` keeps images selected by a person as possible flood or high-water evidence.

Simple example: a reviewer sees a frame where water looks high and saves that frame here for later comparison.

`human-evidence/notes/` keeps simple human review notes.

Simple example: `The water is close to the lower bridge marker at 00:17.`

`outputs/records.jsonl` keeps JSON Lines records created by local POC runs.

`outputs/summary.md` keeps a readable summary report.

`outputs/operator-notes.txt` keeps plain-language output notes.

`outputs/label-comparison.md` keeps the human-label comparison report when you save one.

`outputs/threshold-tuning.md` keeps the prototype threshold tuning report when you save one.

`outputs/review-images/` keeps images generated by OpenFloodAI, such as baseline, changed, and comparison images.

## Human Evidence Compared With System Output

Keep human-selected images separate from system-generated review images.

Simple example:

```text
human-evidence/flood-images/
  frame-human-says-high-water.png

outputs/review-images/run-001/
  review-baseline.png
  review-changed.png
  review-comparison.png
```

This lets us compare:

```text
Human reviewer: this frame looks risky.
OpenFloodAI POC: this frame had the biggest selected-region change.
```

That comparison will help later with labels, QA, and ML training.

## Scratch Runs

Use `data/local-runs/` only for quick temporary runs.

Simple example: a smoke test can write to `data/local-runs/smoke-test/` because it is only checking that the code runs.

For real validation review, prefer `data/sites/<site-name>/`.

## Multi-Video Validation

Use the site validation runner when a site has more than one local video.

```bash
python3 scripts/run_site_validation.py \
  --site-dir data/sites/example-site
```

Simple meaning: the runner finds videos in `inputs/videos/`, runs the current local review workflow for each one, compares matching labels when possible, and writes one combined report.

The combined `validation-report.md` includes:

- totals for processed videos, failed or missing videos, `agree`, `disagree`, and `cannot_compare`
- a summary table with one row per video
- detailed per-window comparison notes

Outputs are grouped by video:

```text
outputs/
  validation-report.md
  rising-001/
    records.jsonl
    summary.md
    operator-notes.txt
    review-images/
    label-comparison.md
```

Missing labels, bad videos, and unclear cases stay visible as `cannot_compare`. They are not counted as success.

If one video has multiple human label time windows, the combined report lists each label window under that video.

Current limit: each label window is compared with the broad system result for the whole video. Later we should compare labels with system output from the same time window.

## Privacy Rules

Do not commit private real videos or real camera images by default.

Good to commit:

- this README
- `.gitkeep` placeholder files
- safe synthetic examples
- public documentation

Do not commit by default:

- real private camera videos
- real flood images from private cameras
- exact sensitive GPS coordinates
- private camera URLs
- generated review images from real footage
- personal contact details

Simple rule: if a file came from a real place or a real camera, keep it local unless it is clearly approved for public use.

## Current Boundary

This folder structure does not add flood detection, ML training, alerts, cloud upload, live cameras, or a database.

It only creates a safe place to organize future validation data and review outputs.
